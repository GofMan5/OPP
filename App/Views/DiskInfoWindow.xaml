<Window x:Class="App.Views.DiskInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:App.Views"
        mc:Ignorable="d"
        Title="Информация о дисках" 
        Height="620" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Стили для кнопок управления окном -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4"
                                Padding="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#313244"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f38ba8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#f38ba880"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Шаблон для карточки диска -->
        <DataTemplate x:Key="DiskCardTemplate">
            <Border Background="#1e1e2e" 
                    CornerRadius="10" 
                    Padding="15" 
                    Margin="0,0,0,10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.2" Color="#000000"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="42"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Иконка диска -->
                    <Border Grid.Column="0" 
                            Background="#313244" 
                            Width="36" 
                            Height="36" 
                            CornerRadius="6" 
                            VerticalAlignment="Top">
                        <Path x:Name="DiskIcon" 
                              Data="{Binding IconPath}" 
                              Fill="#cdd6f4" 
                              Width="20" 
                              Height="20" 
                              Stretch="Uniform"/>
                    </Border>
                    
                    <!-- Информация о диске -->
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <!-- Название и буква диска -->
                        <TextBlock>
                            <Run Text="{Binding DriveLetter}" FontSize="16" FontWeight="SemiBold" Foreground="#f8f8f2"/>
                            <Run Text="{Binding Type}" FontSize="13" Foreground="#CAA9FE"/>
                        </TextBlock>
                        
                        <!-- Использовано/всего -->
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{Binding UsedSpace}" Foreground="#cdd6f4" FontSize="13"/>
                            <TextBlock Text=" из " Foreground="#cdd6f4" FontSize="13"/>
                            <TextBlock Text="{Binding TotalSpace}" Foreground="#f8f8f2" FontSize="13"/>
                        </StackPanel>
                        
                        <!-- Индикатор использования -->
                        <Grid Margin="0,6,0,0" Height="6">
                            <Border Background="#313244" CornerRadius="3"/>
                            <Border HorizontalAlignment="Left" Background="{Binding UsageColorHex}" CornerRadius="3">
                                <Border.Width>
                                    <MultiBinding>
                                        <Binding Path="UsagePercent"/>
                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                                        <MultiBinding.Converter>
                                            <local:SimpleProgressConverter/>
                                        </MultiBinding.Converter>
                                    </MultiBinding>
                                </Border.Width>
                            </Border>
                        </Grid>
                        
                        <!-- Свободно и файловая система -->
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="Свободно: " Foreground="#cdd6f4" FontSize="12"/>
                            <TextBlock Text="{Binding FreeSpace}" Foreground="#f8f8f2" FontSize="12"/>
                            <TextBlock Text=" • " Foreground="#666980" Margin="6,0"/>
                            <TextBlock Text="{Binding FileSystem}" Foreground="#cdd6f4" FontSize="12"/>
                        </StackPanel>
                        
                        <!-- Кнопки управления диском -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button Tag="{Binding DriveLetter}" 
                                    Click="ManualCleanupButton_Click"
                                    Width="170" Height="30" 
                                    Cursor="Hand"
                                    ToolTip="Запуск WinDirStat для анализа занимаемого места">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ButtonBorder" 
                                                Background="#313244" 
                                                CornerRadius="4" 
                                                BorderThickness="1"
                                                BorderBrush="#45475a"
                                                Padding="8,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="0" 
                                                      Data="M12 20C7.59 20 4 16.41 4 12C4 7.59 7.59 4 12 4C16.41 4 20 7.59 20 12C20 16.41 16.41 20 12 20M12 2A10 10 0 0 0 2 12A10 10 0 0 0 12 22A10 10 0 0 0 22 12A10 10 0 0 0 12 2M13 7H11V13H17V11H13V7Z" 
                                                      Width="16" 
                                                      Height="16" 
                                                      Stretch="Uniform" 
                                                      Fill="#cdd6f4" 
                                                      Margin="0,0,6,0"/>
                                                <TextBlock Grid.Column="1" 
                                                           Text="Мануальная очистка" 
                                                           Foreground="#cdd6f4" 
                                                           FontSize="12" 
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#45475a"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#585b70"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Процент использования -->
                    <Border Grid.Column="2" 
                            Background="{Binding UsageColorHex}"
                            CornerRadius="4" 
                            Padding="6,2" 
                            Height="22"
                            VerticalAlignment="Top">
                        <TextBlock Text="{Binding UsagePercent, StringFormat={}{0}%}" 
                                   Foreground="White" 
                                   FontSize="11"
                                   FontWeight="SemiBold"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>
    
    <Border CornerRadius="12" Padding="0">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1e1e2e" Offset="0"/>
                <GradientStop Color="#181825" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.5" Color="#121212"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Строка заголовка -->
            <Border Grid.Row="0" Background="#11111b" MouseDown="TitleBar_MouseDown" CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="1" 
                               Text="Информация о дисках" 
                               Foreground="#cdd6f4" 
                               FontSize="15" 
                               FontWeight="SemiBold" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>
                    
                    <!-- Кнопки управления окном -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,8,0">
                        <!-- Кнопка возврата к главному меню -->
                        <Button x:Name="BackButton" 
                                Style="{StaticResource WindowControlButton}"
                                Click="BackButton_Click"
                                Margin="3,0,3,0"
                                ToolTip="Вернуться в главное меню">
                            <Grid>
                                <TextBlock Text="&#xE72B;" 
                                          FontFamily="Segoe MDL2 Assets" 
                                          Foreground="#cdd6f4"/>
                            </Grid>
                        </Button>
                        
                        <!-- Кнопка Закрыть -->
                        <Button x:Name="CloseButton" 
                                Style="{StaticResource CloseButtonStyle}"
                                Click="CloseButton_Click"
                                ToolTip="Закрыть">
                            <Grid>
                                <Path Data="M0,0 L10,10" 
                                     Stroke="#cdd6f4" 
                                     StrokeThickness="1.6" 
                                     StrokeStartLineCap="Round" 
                                     StrokeEndLineCap="Round"/>
                                <Path Data="M0,10 L10,0" 
                                     Stroke="#cdd6f4" 
                                     StrokeThickness="1.6" 
                                     StrokeStartLineCap="Round" 
                                     StrokeEndLineCap="Round"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Основное содержимое -->
            <Grid Grid.Row="1" Margin="20,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Заголовок и общая информация -->
                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Обзор дисков" 
                               Foreground="#f8f8f2" 
                               FontSize="22" 
                               FontWeight="Bold"/>
                    <TextBlock Text="Здесь отображается информация обо всех дисках вашей системы" 
                               Foreground="#cdd6f4" 
                               FontSize="14" 
                               Margin="0,5,0,0"/>
                </StackPanel>
                
                <!-- Список дисков -->
                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="DisksList" 
                                  ItemTemplate="{StaticResource DiskCardTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Индикатор загрузки данных о дисках -->
                <Grid x:Name="LoadingPanel" Grid.Row="1" Visibility="Visible">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Ellipse Width="40" 
                                 Height="40" 
                                 Margin="0,0,0,15" 
                                 StrokeThickness="3">
                            <Ellipse.Stroke>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#70a1ff" Offset="0"/>
                                    <GradientStop Color="#7bed9f" Offset="1"/>
                                </LinearGradientBrush>
                            </Ellipse.Stroke>
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="LoadingRotation" CenterX="20" CenterY="20"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                        <TextBlock Text="Загрузка информации о дисках..." 
                                   Foreground="#cdd6f4" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
            
            <!-- Нижняя панель с кнопкой обновления -->
            <Border Grid.Row="2" 
                    Background="#11111b" 
                    CornerRadius="0,0,12,12">
                <Grid>
                    <Button x:Name="RefreshButton" 
                            Content="Обновить" 
                            Click="RefreshButton_Click"
                            Width="120" 
                            Height="36" 
                            Foreground="White" 
                            FontSize="14" 
                            FontWeight="Medium" 
                            HorizontalAlignment="Right" 
                            Margin="0,0,20,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="ButtonBorder" 
                                        Background="#7bed9f" 
                                        CornerRadius="6" 
                                        Padding="15,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" 
                                              Data="M12 4V1L8 5l4 4V6c3.31 0 6 2.69 6 6 0 2.97-2.17 5.43-5 5.91v2.02c3.95-.49 7-3.85 7-7.93 0-4.42-3.58-8-8-8zm-6 8c0-1.65.67-3.15 1.76-4.24L6.34 6.34A8.014 8.014 0 0 0 4 12c0 4.08 3.05 7.44 7 7.93v-2.02c-2.83-.48-5-2.94-5-5.91z" 
                                              Width="16" 
                                              Height="16" 
                                              Stretch="Uniform" 
                                              Fill="White" 
                                              Margin="0,0,8,0"/>
                                        <ContentPresenter Grid.Column="1" 
                                                          HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#79E2A6"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#6AD090"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window> 