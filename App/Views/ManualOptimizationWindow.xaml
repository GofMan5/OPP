<Window x:Class="App.Views.ManualOptimizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:App.Views"
        mc:Ignorable="d"
        Title="Мануальная оптимизация"
        Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
        
    <Window.Resources>
        <!-- Общие эффекты -->
        <DropShadowEffect x:Key="CommonShadowEffect" BlurRadius="10" ShadowDepth="2" Opacity="0.15" Color="#000000"/>
        <DropShadowEffect x:Key="CheckBoxHoverEffect" BlurRadius="8" ShadowDepth="0" Opacity="0.3" Color="#89dceb"/>
        
        <!-- Стили для кнопок управления окном -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8"
                                Padding="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#313244"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#45475a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f38ba8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#f38ba880"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Стиль для CheckBox -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="Margin" Value="0,12,0,12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border x:Name="CheckBoxBorder" 
                                    Width="22" 
                                    Height="22" 
                                    CornerRadius="6" 
                                    BorderThickness="2" 
                                    Background="Transparent"
                                    BorderBrush="#7f849c"
                                    Margin="0,0,12,0"
                                    Effect="{StaticResource CommonShadowEffect}">
                                <Path x:Name="CheckMark"
                                      Data="M4,10 L8,15 L16,6" 
                                      Stroke="White" 
                                      StrokeThickness="2.5" 
                                      StrokeStartLineCap="Round" 
                                      StrokeEndLineCap="Round" 
                                      Visibility="Hidden"/>
                            </Border>
                            
                            <ContentPresenter Grid.Column="1" 
                                             VerticalAlignment="Center" 
                                             HorizontalAlignment="Left" 
                                             RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#74c7ec"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#74c7ec"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#89dceb"/>
                                <Setter TargetName="CheckBoxBorder" Property="Effect" Value="{StaticResource CheckBoxHoverEffect}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для заголовков категорий -->
        <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#f9f9fa"/>
            <Setter Property="Margin" Value="0,8,0,16"/>
        </Style>
        
        <!-- Стиль для описаний твиков -->
        <Style x:Key="TweakDescriptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#a6adc8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="34,0,0,15"/>
            <Setter Property="LineHeight" Value="20"/>
        </Style>
        
        <!-- Стиль для кнопки применения -->
        <Style x:Key="ApplyButton" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="25,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1A76D2" Offset="0"/>
                                    <GradientStop Color="#4597E0" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" 
                                                 Direction="315" 
                                                 Opacity="0.2" 
                                                 ShadowDepth="2" 
                                                 Color="Black"/>
                            </Border.Effect>
                            <Border.RenderTransform>
                                <TranslateTransform X="0" Y="0"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="1" Y="-1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="16" 
                                                         Direction="315" 
                                                         Opacity="0.3" 
                                                         ShadowDepth="3" 
                                                         Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="0.5" Y="-0.5"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" 
                                                         Direction="315" 
                                                         Opacity="0.2" 
                                                         ShadowDepth="1" 
                                                         Color="Black"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#45475a"/>
                                <Setter Property="Foreground" Value="#a6adc8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Стиль для панелей категорий -->
        <Style x:Key="TweakPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="#1e1e2e"/>
            <Setter Property="BorderBrush" Value="#313244"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="25,20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect" Value="{StaticResource CommonShadowEffect}"/>
        </Style>
        
        <!-- Стиль для кнопок выбора -->
        <Style x:Key="SelectButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#89b4fa"/>
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#45475a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#585b70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="16" Padding="0">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#181825" Offset="0"/>
                <GradientStop Color="#11111b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="#121212"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Строка заголовка с кнопками управления окном -->
            <Border Grid.Row="0" Background="#11111b" MouseDown="TitleBar_MouseDown" CornerRadius="16,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="1" 
                              Text="Мануальная оптимизация" 
                              Foreground="#f9f9fa" 
                              FontSize="16" 
                              FontWeight="SemiBold" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                    
                    <!-- Кнопки управления окном -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,14,0">
                        <!-- Кнопка Закрыть -->
                        <Button x:Name="CloseButton" 
                                Style="{StaticResource CloseButtonStyle}"
                                Click="CloseButton_Click"
                                Margin="3,0,0,0"
                                ToolTip="Закрыть">
                            <Grid>
                                <Path Data="M0,0 L10,10" 
                                     Stroke="#f9f9fa" 
                                     StrokeThickness="1.8" 
                                     StrokeStartLineCap="Round" 
                                     StrokeEndLineCap="Round"/>
                                <Path Data="M0,10 L10,0" 
                                     Stroke="#f9f9fa" 
                                     StrokeThickness="1.8" 
                                     StrokeStartLineCap="Round" 
                                     StrokeEndLineCap="Round"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Основное содержимое - пустой шаблон для оптимизаций -->
            <ScrollViewer Grid.Row="1" 
                         Margin="25,15,25,0"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <!-- Счетчик выбранных твиков -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock x:Name="SelectedTweaksCountText"
                                  Text="Выбрано: 0 оптимизаций" 
                                  FontSize="15" 
                                  Foreground="#cdd6f4" 
                                  VerticalAlignment="Center"/>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="SelectAllButton"
                                   Content="Выбрать все" 
                                   Style="{StaticResource SelectButtonStyle}"
                                   Click="SelectAllButton_Click"/>
                            
                            <Button x:Name="UnselectAllButton"
                                   Content="Снять все" 
                                   Style="{StaticResource SelectButtonStyle}"
                                   Click="UnselectAllButton_Click"
                                   Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Пустой шаблон категории для новых оптимизаций -->
                    <Border Style="{StaticResource TweakPanelStyle}">
                        <StackPanel>
                            <TextBlock Text="Выберите оптимизации" 
                                      Style="{StaticResource CategoryHeaderStyle}"
                                      FontSize="20"
                                      HorizontalAlignment="Center"
                                      Margin="0,50,0,20"/>
                            
                            <TextBlock Text="Оптимизации будут добавлены позже. Выберите желаемые настройки для улучшения производительности вашей системы."
                                      Style="{StaticResource TweakDescriptionStyle}"
                                      FontSize="15"
                                      HorizontalAlignment="Center"
                                      Margin="25,10,25,60"
                                      TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Нижняя панель с кнопкой применения -->
            <Border Grid.Row="2" Background="#11111b" Padding="25,18" CornerRadius="0,0,16,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Предупреждение о создании точки восстановления -->
                    <CheckBox x:Name="CreateRestorePointCheckBox" 
                             Content="Создать точку восстановления перед применением" 
                             IsChecked="True"
                             Style="{StaticResource ModernCheckBox}" 
                             Grid.Column="0"/>
                    
                    <!-- Кнопка применения выбранных оптимизаций -->
                    <Button Grid.Column="1" 
                           Content="Применить оптимизации" 
                           x:Name="ApplyOptimizationsButton"
                           Style="{StaticResource ApplyButton}" 
                           Click="ApplyOptimizationsButton_Click"
                           IsEnabled="False"/>
                </Grid>
            </Border>
            
            <!-- Индикатор загрузки -->
            <Border x:Name="LoadingPanel" 
                   Grid.RowSpan="3" 
                   Background="#11111be6"
                   Visibility="Collapsed"
                   CornerRadius="16">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Background="#1e1e2e" 
                               CornerRadius="16" 
                               Padding="35,30" 
                               Margin="0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" 
                                                 ShadowDepth="0" 
                                                 Opacity="0.5" 
                                                 Color="#000000"/>
                            </Border.Effect>
                            <StackPanel>
                                <Ellipse Width="60" 
                                       Height="60" 
                                       Margin="0,0,0,25" 
                                       StrokeThickness="4">
                                    <Ellipse.Stroke>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#1A76D2" Offset="0"/>
                                            <GradientStop Color="#4597E0" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Stroke>
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="LoadingRotation" CenterX="30" CenterY="30"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <TextBlock x:Name="LoadingText"
                                         Text="Применение оптимизаций..." 
                                         FontSize="18" 
                                         Foreground="White" 
                                         FontWeight="Medium"
                                         HorizontalAlignment="Center"
                                         Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal" 
                                           HorizontalAlignment="Center">
                                    <Ellipse x:Name="Dot1" 
                                            Width="8" 
                                            Height="8" 
                                            Fill="#f8f8f2" 
                                            Margin="4,0"/>
                                    <Ellipse x:Name="Dot2" 
                                            Width="8" 
                                            Height="8" 
                                            Fill="#f8f8f2" 
                                            Margin="4,0"/>
                                    <Ellipse x:Name="Dot3" 
                                            Width="8" 
                                            Height="8" 
                                            Fill="#f8f8f2" 
                                            Margin="4,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window> 